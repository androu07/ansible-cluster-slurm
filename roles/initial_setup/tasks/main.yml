---
# ===== Setup inicial para conectividad =====

- name: Actualizar cache de apt
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600
  tags:
    - update_cache

- name: Instalar sshpass
  ansible.builtin.apt:
    name: sshpass
    state: present
  tags:
    - install_sshpass

- name: Instalar rng-tools
  ansible.builtin.apt:
    name: rng-tools
    state: present
  tags:
    - install_rngtools

