apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://{{ hostvars[groups['headnode'][0]]['ansible_host'] }}:9090
    isDefault: true
    editable: false